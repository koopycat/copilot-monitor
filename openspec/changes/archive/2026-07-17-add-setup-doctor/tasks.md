## 1. Specification and command surface

- [x] 1.1 Add the setup-diagnostics delta specification and synchronize the
      reporting capability after implementation.
- [x] 1.2 Add `doctor` dispatch, usage, and zsh completion entries.

## 2. Diagnostic implementation

- [x] 2.1 Implement side-effect-free database inspection and bounded health
      endpoint checks.
- [x] 2.2 Implement optional upstream TCP checking and actionable human/JSON
      report rendering.
- [x] 2.3 Add deterministic unit tests for success, failure, JSON, invalid
      input, and no-database mutation behavior.

## 3. Documentation and verification

- [x] 3.1 Add `doctor` to the README and landing-page first-capture paths.
- [x] 3.2 Run targeted tests, formatting, markdown lint, build, and the
      repository fast test suite.
