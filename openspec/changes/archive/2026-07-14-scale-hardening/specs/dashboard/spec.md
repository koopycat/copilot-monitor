## ADDED Requirements

### Requirement: Session pagination and filtering

The dashboard SHALL support paginated session browsing with project and
date-range filtering.

#### Scenario: Load more sessions

- **WHEN** the user clicks "Load more" at the bottom of the sessions table
- **THEN** the next page of sessions is loaded and appended to the table

#### Scenario: Filter sessions by project

- **WHEN** the user selects a project from the sessions project filter dropdown
- **THEN** the sessions table shows only sessions belonging to that project

#### Scenario: Filter sessions by date range

- **WHEN** the user selects a date range in the sessions filter
- **THEN** the sessions table shows only sessions within that date range

#### Scenario: Session count display

- **WHEN** sessions are loaded
- **THEN** the sessions section shows "Showing X-Y of Z sessions" where Z is the
  total count

---

### Requirement: Model usage chart grouping

The dashboard usage chart SHALL group models below a 5% contribution threshold
into an "Other" category.

#### Scenario: Many models in chart

- **WHEN** more than 10 distinct models appear in the timeline data
- **THEN** models contributing less than 5% of total tokens or requests are
  aggregated into an "Other" category in the stacked bar chart

#### Scenario: Few models in chart

- **WHEN** 10 or fewer distinct models appear
- **THEN** all models are shown individually without grouping

---

### Requirement: Scalable chart color palette

The usage chart SHALL generate visually distinct colors for each model category
using HSL color interpolation, supporting any number of categories without color
reuse.

#### Scenario: More than 7 models

- **WHEN** 15 distinct model categories need colors
- **THEN** each category receives a visually distinct color generated by
  interpolating around the HSL hue wheel

---

### Requirement: Anomaly feed in dashboard

The dashboard SHALL display a feed of recent anomalies with severity badges.

#### Scenario: Anomalies present

- **WHEN** anomalies exist and the dashboard loads
- **THEN** a collapsible anomaly feed section shows the 10 most recent anomalies
  with severity (info/warn/error), category, timestamp, and detail

#### Scenario: No anomalies

- **WHEN** no anomalies exist
- **THEN** the anomaly feed section shows "No anomalies detected"

## MODIFIED Requirements

### Requirement: Recent sessions table

The dashboard SHALL display sessions in a table with start time, duration,
project label, request count, token count, and estimated cost. The table SHALL
support cursor-based pagination with a "Load more" button, and SHALL support
filtering by project and date range. The Recent Sessions section SHALL be
expanded when the dashboard first loads and independently collapsible
thereafter.

#### Scenario: Sessions table

- **WHEN** sessions exist in the database and the Recent Sessions section is
  expanded
- **THEN** they are listed in reverse chronological order with summary fields,
  with a "Load more" button visible when more sessions exist

#### Scenario: Recent Sessions section collapsed

- **WHEN** the user collapses the Recent Sessions section
- **THEN** the sessions table is hidden while the Recent Sessions section
  control remains visible

### Requirement: Per-model breakdown

The dashboard SHALL display a per-model breakdown table showing model names,
request counts, token counts, cache hit ratios, latency, and estimated cost per
model. The table SHALL support sorting by clicking column headers. The Models
section SHALL be expanded when the dashboard first loads and independently
collapsible thereafter.

#### Scenario: Models table

- **WHEN** captured data exists and the Models section is expanded
- **THEN** the table shows one row per model with requests, tokens, cache
  percentage, average latency, and cost, sorted by total tokens descending by
  default

#### Scenario: Sort models by column

- **WHEN** the user clicks a column header (e.g., "Total $")
- **THEN** the table re-sorts by that column; clicking again reverses the sort
  order

#### Scenario: Models section collapsed

- **WHEN** the user collapses the Models section
- **THEN** the model table is hidden while the Models section control remains
  visible

### Requirement: Refresh feedback

The dashboard SHALL provide visual feedback during data refresh: a loading state
on the refresh button and a "Updated" timestamp flash when data changes.

#### Scenario: Refresh button during load

- **WHEN** a manual or auto-refresh is in progress
- **THEN** the refresh button shows a spinning or pulsing indicator

#### Scenario: Auto-refresh guard

- **WHEN** the auto-refresh timer fires but a previous refresh is still in
  progress
- **THEN** the new refresh is skipped (no overlapping requests)

#### Scenario: Timestamp flash on update

- **WHEN** new data arrives from a refresh
- **THEN** the "Updated" timestamp briefly highlights or flashes to draw
  attention
